// This function uses `export default` to allow it to be the default export of the module
export default function taskBlock() {
  // Declare two constants `task` and `task2` with boolean values
  const task = false;
  const task2 = true;

  // Return an array containing the two constants
  return [task, task2];
}

