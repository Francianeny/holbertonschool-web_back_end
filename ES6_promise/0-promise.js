export default function getResponseFromAPI() {
  // La fonction retourne un nouvel objet Promise
  return new Promise((resolve) => {
    // La fonction resolve est appelée immédiatement, remplissant ainsi la promesse
    resolve();
  });
}
